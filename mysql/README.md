# MySQL

MySQL 관련 스키마, 마이그레이션, 시드 파일을 보관하는 폴더입니다.

현재 애플리케이션 코드에는 MySQL 연결 코드가 없으므로, 실제 데이터베이스 연동을 추가할 때 아래와 같이 파일을 분리해서 관리하는 것을 권장합니다.

```text
mysql/
  migrations/   # 테이블 생성 및 변경 SQL
  seeds/        # 초기 데이터 SQL
```
